apiVersion: apps/v1
kind: Deployment
metadata:
  name: nextcloud
spec:
  template:
    spec:
      containers:
        - name: nextcloud
          volumeMounts:
            - mountPath: /var/www/html/data
              $patch: delete
        - name: nextcloud-cron
          volumeMounts:
            - mountPath: /var/www/html/data
              $patch: delete